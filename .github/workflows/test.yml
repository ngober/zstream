name: Run Tests

on:
 - 'push'
 - 'pull_request'
 
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: g++ --std=c++17 main.cc -lz
    - name: run
      run: ./a.out

